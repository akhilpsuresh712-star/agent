fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0   # loads .env (LLM keys) on startup; optional at runtime
# Optional — only needed for the LLM planner (PROCUREMENT_PLANNER=llm).
# OpenAI SDK pointed at any OpenAI-compatible endpoint (default: Groq):
# openai>=1.40
